// Example functionality for buttons (if needed)
document.querySelectorAll('.btn-outline-light').forEach(button => {
    button.addEventListener('click', () => {
        alert('You clicked a button!');
    });
});
